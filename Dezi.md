# Dezi

## Changes I made

I fixed the power_of() function by replacing multiplication (`*`) with the exponent operator (`**`).
Fixed the list_total() function so it correctly calculates the sum of all numbers in the list.
Fixed the average_of_list() function so it divides the total by the number of elements in the list.

## Conflicts
 No merge conflict occurred to me.
